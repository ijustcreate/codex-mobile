let timer;
export function render(){clearInterval(timer);queueMicrotask(start);return `<section class="mini-app lantern-app"><div class="lantern-hero"><p class="eyebrow">Tiny Quest Lanterns</p><h1>A cozy quest<br>for everyone.</h1><p>Create a room or join family with a six-letter code.</p><div class="lantern-actions"><button id="quest-create">Create quest</button><input id="quest-code" maxlength="6" placeholder="ROOM CODE"><button id="quest-join">Join quest</button></div></div><div id="quest-game"></div></section>`}
function start(){let code=localStorage.getItem("lantern-room");const game=document.querySelector("#quest-game");if(!game)return;const call=(url,body)=>fetch(url,{method:body?"POST":"GET",headers:{"Content-Type":"application/json"},body:body?JSON.stringify(body):undefined}).then(r=>r.json());
async function show(room){code=room.code;localStorage.setItem("lantern-room",code);const active=room.players[room.turn];game.innerHTML=`<div class="quest-head"><b>ROOM ${room.code}</b><span>${active.name}'s turn</span></div><div class="quest-board">${room.tiles.map((t,i)=>`<div class="quest-tile ${t.revealed?t.type:"hidden"}">${t.revealed?icon(t.type):"?"}${room.players.filter(p=>p.pos===i).map(p=>`<i title="${p.name}">${p.name[0]}</i>`).join("")}</div>`).join("")}</div><div class="quest-controls"><button data-q="move">Move + Explore</button><button data-q="help">Help Everyone</button></div><div class="player-rail">${room.players.map(p=>`<article><b>${p.name}</b><small>${p.hero}</small><span>STAR ${p.stars} · KIND ${p.kindness}</span></article>`).join("")}</div><div class="quest-log">${room.log.slice(-4).reverse().map(x=>`<p>${x}</p>`).join("")}</div>`;game.querySelectorAll("[data-q]").forEach(b=>b.onclick=async()=>show((await call("/api/quest/action",{code,action:b.dataset.q})).room))}
document.querySelector("#quest-create").onclick=async()=>show((await call("/api/quest/create",{name:"Guide"})).room);document.querySelector("#quest-join").onclick=async()=>show((await call("/api/quest/join",{code:document.querySelector("#quest-code").value,name:"Explorer",hero:"Mushroom Wizard"})).room);if(code){call(`/api/quest/${code}`).then(x=>x.room&&show(x.room));timer=setInterval(()=>call(`/api/quest/${code}`).then(x=>x.room&&show(x.room)),1500)}}
function icon(t){return{path:"·",treasure:"★",monster:"!",quest:"?",spring:"~",campfire:"▲"}[t]}
